/**
 * Builder document schema versioning.
 * Increment CURRENT when making backward-incompatible JSON shape changes.
 */
export const LEGACY_BUILDER_SCHEMA_VERSION = 0;
export const CURRENT_BUILDER_SCHEMA_VERSION = 1;
