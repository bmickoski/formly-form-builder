name: Pages

on:
  push:
    branches: [master, main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build for GitHub Pages
        run: npm run build:pages

      - name: Build documentation
        run: npm run docs

      - name: Build Storybook
        run: npm run storybook:build

      - name: Publish docs site under /docs, Compodoc under /docs/api, and Storybook under /docs/storybook
        run: |
          mkdir -p dist/formly-form-builder/browser/docs
          mkdir -p dist/formly-form-builder/browser/docs/api
          mkdir -p dist/formly-form-builder/browser/docs/storybook
          cp docs/site/index.html dist/formly-form-builder/browser/docs/index.html
          cp -r docs/compodoc/* dist/formly-form-builder/browser/docs/api/
          cp -r storybook-static/* dist/formly-form-builder/browser/docs/storybook/

      - name: SPA fallback
        run: cp dist/formly-form-builder/browser/index.html dist/formly-form-builder/browser/404.html

      - name: Upload pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist/formly-form-builder/browser

  deploy:
    if: github.ref == 'refs/heads/master' || github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
