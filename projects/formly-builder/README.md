# @ngx-formly-builder/core

Embeddable Angular standalone Formly visual builder.

## Usage

```ts
import { FormlyBuilderComponent } from '@ngx-formly-builder/core';
```

```html
<formly-builder [config]="config" (configChange)="config = $event" />
```
